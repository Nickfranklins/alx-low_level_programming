#include "main.h"
/**
 * _abs - computer the absolute value of an integer
 * @n: integer type number
 * Return: absolute value of @n
 */
int _abs (int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		retun (n);
	}
}
